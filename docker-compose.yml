version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
      - ENVIRONMENT=development
      - FLASK_DEBUG=1
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - YOUR_DOMAIN=${YOUR_DOMAIN:-https://3bb3b4695c77.ngrok-free.app}
      - REPLIT_DEV_DOMAIN=3bb3b4695c77.ngrok-free.app
    command: python main.py

# Removed db service and depends_on

# volumes:
#   postgres_data: 